# Agents

Multiple agents work the XP Labs workspace in turns. The rules live in
`../CLAUDE.md` at the workspace root: pull with `bash sync.sh` before any
work, push with `bash sync.sh push "msg"` the moment work is done. GitHub
is the truth; the folder is a scratchpad.
